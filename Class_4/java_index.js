let maxNumbers = 3
let maxCounter = 0
maxNum = prompt("user input")
while (counter < maxNumbers - 1) {
    let num = Number(prompt("user input"))
    if (num > maxNumbers) {
        maxCounter = counter
    }

}


